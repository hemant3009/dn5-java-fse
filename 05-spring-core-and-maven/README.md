# Module 5 — Spring Core & Maven
**Construct:** Products & Frameworks

Maven Spring project (`spring-core-maven-handson/`). Start of the **Country** thread that continues into Modules 6a and 7.

## Mandatory hands-on
- [ ] **Exercise 1: Configuring a Basic Spring Application** — `Country` bean in XML, loaded via `ApplicationContext`.
- [ ] **Exercise 2: Implementing Dependency Injection** — wire one bean into another via XML.
- [ ] **Exercise 4: Creating and Configuring a Maven Project** — `pom.xml` with Spring deps; builds cleanly.

## How to run
```bash
cd spring-core-maven-handson && mvn compile exec:java
```
