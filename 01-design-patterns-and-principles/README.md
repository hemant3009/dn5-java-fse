# Module 1 — Design Patterns & Principles
**Construct:** Engineering Concepts

## Mandatory hands-on
- [ ] **Exercise 1: Singleton Pattern** — private constructor, static instance, `getInstance()`; `main` proves a single shared instance.
- [ ] **Exercise 2: Factory Method Pattern** — product interface, concrete implementations, factory returning the correct type by input.

## How to run
```bash
javac *.java && java <MainClass>
```

## Notes
Self-contained Java, no build tool required.
