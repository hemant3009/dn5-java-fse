# Module 6 — Spring Data JPA with Spring Boot, Hibernate
**Construct:** Products & Frameworks

Spring Boot project (`spring-data-jpa-handson/`), Country domain.

## Mandatory hands-on
- [ ] **Spring Data JPA — Quick Example** — `Country` entity + `JpaRepository`, save/fetch against H2.
- [ ] **Difference between JPA, Hibernate and Spring Data JPA** — written explanation (see `NOTES.md`).

## How to run
```bash
cd spring-data-jpa-handson && mvn spring-boot:run
```
